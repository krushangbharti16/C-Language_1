#include <stdio.h>

int main()
{
    int Kru, Pri, Kri, Shree;

    printf("Enter the basic salary: ");
    scanf("%d", &Kri);

    Kru = Shree * 0.1;
    Pri = Shree * 0.05;
    Kri = Shree * 0.08;

    printf("The gross salary is: %d", Shree);
}